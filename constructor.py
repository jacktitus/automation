# define a new type called person this person object must have a name attribute and a talk()

"""class Person:
    def __int__(self, name):
        self.name = name

    def talk(self):
        print("talk")

xy= Person("jack")
print(xy.name)"""


#max
numbers=[10, 20, 40, 50, 80, 3, 2, 0, 9]
maximum = numbers[0]
for number in numbers:
    if number>maximum:
        maximum=number
print(maximum)   # for renaming use refractor or shift + f6 .....to comment use ctrl +/